# Django Configuration
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=1

# Database Configuration - Choose ONE option:

# Option 1: Using DATABASE_URL (recommended for Digital Ocean managed DB)
# Format: mysql://username:password@hostname:port/dbname
# Digital Ocean example (copy from DB Cluster connection details):
# DATABASE_URL=mysql://doadmin:your_password@db-mysql-nyc3-35371-do-user-30778760-0.j.db.ondigitalocean.com:25060/defaultdb

# Option 2: Using individual DB variables (for local/docker-compose development)
DB_NAME=audit_db
DB_USER=audit_user
DB_PASSWORD=audit_password
DB_HOST=db
DB_PORT=3306

# MySQL root (only for docker-compose)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=audit_db
MYSQL_USER=audit_user
MYSQL_PASSWORD=audit_password

# Note: If neither DATABASE_URL nor DB_HOST are set, Django will fall back to SQLite